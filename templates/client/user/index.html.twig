{% extends '../client/base.html.twig' %}

{% block title %}Articles{% endblock %}

{% block body %}
    {% for user in users %}

            {{ user.username }}
            {{ user.email }}
            {{ user.isVerified }}
            {{ user.image }}
            {{ user.roles[0] }}

    {% endfor %}
{% endblock %}

