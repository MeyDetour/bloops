{% extends 'base.html.twig' %}



{% form_theme form 'bootstrap_5_layout.html.twig' %}

{% block body %}

            {{ form_start(form)}}
            {{ form_row(form.titre) }}
            {{ form_row(form.audioFile) }}
            {{ form_row(form.description) }}
            <button type="submit" class="btn btn-success">Ajouter le son</button>
            {{ form_end(form) }}

{% endblock %}
