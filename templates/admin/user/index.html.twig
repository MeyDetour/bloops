{% extends 'admin/base.html.twig' %}

{% block title %}Articles{% endblock %}

{% block body %}

    {% for user in users %}

        {{ user.id }}
        {{ user.username }}
        {{ user.email }}
        {{ user.isVerified }}
        {{ user.password }}
        {{ user.image }}
        {{ user.roles[0] }}


    {% endfor %}

{% endblock %}

